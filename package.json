{
  "name": "kc-portfolio",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "npm run sync && npm run images && astro build",
    "preview": "astro preview",
    "images": "node scripts/build-images.mjs",
    "sync": "node scripts/sync-images.mjs",
    "studio": "npm --prefix studio run dev",
    "studio:deploy": "npm --prefix studio run deploy"
  },
  "dependencies": {
    "@sanity/client": "^8.3.0",
    "astro": "^5.14.1",
    "sharp": "^0.34.4"
  }
}
