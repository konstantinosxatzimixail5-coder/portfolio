{
  "name": "kc-portfolio",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "predev": "npm run top-up",
    "prebuild": "npm run top-up",
    "build": "astro build",
    "preview": "astro preview",
    "top-up": "node scripts/top-up-images.mjs",
    "content": "npm run sync && npm run images",
    "images": "node scripts/build-images.mjs",
    "sync": "node scripts/sync-images.mjs",
    "studio": "npm --prefix studio run dev",
    "studio:deploy": "npm --prefix studio run deploy",
    "seed": "node --experimental-strip-types scripts/seed-sanity.mjs"
  },
  "dependencies": {
    "@sanity/client": "^8.3.0",
    "astro": "^5.14.1",
    "sharp": "^0.34.4"
  }
}
