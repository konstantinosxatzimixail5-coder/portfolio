---
import { getSettings } from '../lib/content';

interface Props {
  active?: string;
}
const { active } = Astro.props;

const settings = await getSettings();
const links = settings.navLinks ?? [];

// The monogram is drawn as three parts so the slash can take the red. It is one
// string in the CMS, split here, because "K/C" is what an editor wants to type.
const [markStart = '', markEnd = ''] = String(settings.monogram ?? '').split('/');
---

<header class="nav">
  <div class="nav__inner shell">
    <a class="nav__mark" href="/" aria-label={`${settings.name}, home`}>
      <span aria-hidden="true">{markStart}</span><span class="nav__slash" aria-hidden="true"
        >/</span
      ><span aria-hidden="true">{markEnd}</span>
    </a>

    <nav class="nav__links" aria-label="Main">
      {
        links.map((l: any) => (
          <a
            href={l.href}
            class="nav__link mono"
            aria-current={active === l.key ? 'page' : undefined}
          >
            {l.label}
          </a>
        ))
      }
    </nav>

    <a class="nav__reel mono" href="/reel/" aria-current={active === 'reel' ? 'page' : undefined}>
      Reel
    </a>
  </div>
</header>

<style>
  .nav {
    position: sticky;
    top: 0;
    z-index: 50;
    background: color-mix(in srgb, var(--ink) 88%, transparent);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--rule);
  }

  .nav__inner {
    display: flex;
    align-items: center;
    gap: var(--s-5);
    min-height: var(--tap);
    padding-block: var(--s-2);
  }

  .nav__mark {
    font-family: var(--display);
    font-size: 1.35rem;
    line-height: 1;
    color: var(--newsprint);
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    min-height: var(--tap);
    padding-right: var(--s-2);
    letter-spacing: 0.02em;
  }

  .nav__slash {
    color: var(--red);
    padding-inline: 0.08em;
  }

  /* Seven items, and every one of them visible at every width. It used to
     scroll sideways when it ran out of room, which hides links behind a gesture
     nobody knows is available: at 320px that silently swallowed the last three.
     Wrapping instead costs a second line at the narrow end and loses nothing. */
  .nav__links {
    display: flex;
    flex-wrap: wrap;
    gap: 0 var(--s-5);
    margin-right: auto;
  }

  .nav__link {
    color: var(--fg-quiet);
    text-decoration: none;
    white-space: nowrap;
    display: inline-flex;
    align-items: center;
    min-height: var(--tap);
    border-bottom: 1px solid transparent;
  }

  .nav__link:hover,
  .nav__link[aria-current='page'] {
    color: var(--newsprint);
    border-bottom-color: var(--red);
  }

  /* Full tap height at every width, not only on a phone. This is the primary
     call to action on the site and a 34px target is mean to a trackpad too. */
  .nav__reel {
    display: inline-flex;
    align-items: center;
    min-height: var(--tap);
    padding: 0 var(--s-4);
    margin-left: var(--s-2);
    color: var(--ink);
    background: var(--cyan);
    border-radius: var(--radius);
    text-decoration: none;
    font-weight: 600;
    white-space: nowrap;
  }

  .nav__reel:hover {
    background: var(--newsprint);
    color: var(--ink);
  }

  /* The seven links stop fitting beside the mark and the reel button well
     before a phone: 768px was already clipping them. The threshold is set where
     the row actually runs out of room, not where a device class begins. */
  @media (max-width: 880px) {
    .nav__inner {
      flex-wrap: wrap;
      gap: var(--s-3);
      padding-bottom: 0;
    }
    .nav__mark {
      margin-right: auto;
    }
    .nav__links {
      order: 3;
      width: 100%;
      gap: 0 var(--s-4);
      margin-right: 0;
      border-top: 1px solid var(--rule);
    }
    .nav__link {
      font-size: 0.72rem;
      min-height: var(--tap);
    }
  }

  /* Tightening the gap buys one more item per line without touching the type
     size, which is the one lever the brief rules out. */
  @media (max-width: 420px) {
    .nav__links {
      gap: 0 var(--s-3);
    }
  }
</style>
